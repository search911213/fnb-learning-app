MONGO_URI=mongodb://127.0.0.1:27017/event_programs
PORT=5000
# BASE_URL should point to a URL reachable by your phone when scanning QR codes.
# Option 1 (LAN): set this to your PC's local IP and port, for example:
# BASE_URL=http://192.168.1.25:5000
# Option 2 (public): use a tunnel like ngrok and set BASE_URL to the forwarding URL, for example:
# BASE_URL=https://abcd1234.ngrok.io
# Default placeholder (change before scanning from another device):
BASE_URL=http://127.0.0.1:5000
